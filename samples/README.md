# Samples

Principles:
- No secrets committed.
- Keep values in local-only files (generated from templates).

## Android

- `samples/Kapusch.Facebook.Android.Sample/` — minimal Android sample that links against the wrapper AAR + Facebook AARs.
